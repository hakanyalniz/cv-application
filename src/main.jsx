import React from "react";
import ReactDOM from "react-dom/client";
import CVFORMAIN from "./components/CVFORMMain.jsx";
import "./styles/index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CVFORMAIN />
  </React.StrictMode>
);
